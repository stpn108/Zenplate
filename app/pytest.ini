[pytest]
asyncio_mode = auto
addopts = --ruff
markers =
    slow: Long-running tests
    integration: Integration tests (may need external services)
    webapp: Web interface tests
    core: Core application tests
